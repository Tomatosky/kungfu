import * as utils from './utils/index';
import * as actions from './io/index';
import * as config from './config/index';

export default {
    actions: actions.default,
    utils: utils.default,
    config: config.default
}

